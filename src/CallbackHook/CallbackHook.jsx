// if some function is reduring without begin invoked then we use callback hook

import React, { useCallback } from 'react'

const CallbackHook = () => { 

    // const addtodo = useCallback(() => {
    //     return setTodods((prev) => [...prev, `new entry`])
    // },[todos]);

  return (
    <div>
      <h5>React Use Callback Hook</h5>

      
    </div>
  )
}

export default CallbackHook
